pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                dir('C:/Jenkins/190624_test.bat') {
                    /* execute commands in the scripts directory */
                }
            }
        }
        stage('Test') {
            steps {
                dir('C:/Jenkins/190624_test.bat') {
                    /* execute commands in the scripts directory */
                }
            }
        }
      stage('Package') {
            steps {
                dir('C:/Jenkins/190624_test.bat') {
                    /* execute commands in the scripts directory */
                }
            }
        }
        stage('Deploy') {
            steps {
                dir('C:/Jenkins/190624_test.bat') {
                    /* execute commands in the scripts directory */
                }
            }
        }
        stage('HAHA_auto_trigger') {
            steps {
                dir('C:/Jenkins/190624_test.bat') {
                    /* execute commands in the scripts directory */
                }
            }
        }
    }
}
